version: "3.8"
services:
  nginx:
    container_name: nginx
    hostname: nginx
    restart: always
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf